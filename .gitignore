node_modules
.next
.env*.local
.DS_Store
*.log

# pastas-wrapper de entrega (não devem ir pro repo nem ser type-checadas)
/fs-app/
/financeiro-simples-app/
fs-app.zip

# capturas da auditoria — geradas por scripts/bancada/capturar.js
auditoria/

# gerado pelo tsc a cada checagem — ruído no diff
*.tsbuildinfo
